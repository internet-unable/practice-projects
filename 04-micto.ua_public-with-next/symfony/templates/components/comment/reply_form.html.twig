{% form_theme form 'form/register_form_layout.html.twig' %}

{{ form_start(form, {
    'action': path('create_reply'),
    'attr': {
        'id': 'reply-form'
    }}) 
}}

    {% if form_errors(form) %}
        <div class="error-message">
            Не задано коментар для відповіді
        </div>
    {% endif %}

{{ form_end(form) }}