{% form_theme form 'form/register_form_layout.html.twig' %}

{{ form_start(form, {
    'action': path('api_create_notify'), 
    'attr': {
        'data-success-msg':"Незабаром ми розглянемо Вашу заявку",
        'id': 'notify-form'
    }})
}}
    {% if form_errors(form) %}
        <div class="error-message wide">
            Виникла помилка при збереженні даних
        </div>
    {% endif %}

{{ form_end(form) }}
