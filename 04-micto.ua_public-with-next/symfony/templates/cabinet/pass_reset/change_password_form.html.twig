{{ form_start(form, {
	'action': path('change_password_page', {
		'code': code
	}),
	'attr': {
		'id': 'pass-reset-form'
	}
})}}

	{{ form_row(form.plain_password) }}

	{{ form_row(form.submit, {
		'attr': {
			'class': 'button'
		}
	})}}

{{ form_end(form) }}