import * as React from 'react';
import CabinetSettingForm from '../../component/Forms/CabinetSettingForm';

type Props = {};

export default function Settings(props: Props) {
  React.useEffect(() => {
    //
  }, []);

  // if (isLoading) {
  //   <div>Loading...</div>;
  // }

  return <CabinetSettingForm />;
}
